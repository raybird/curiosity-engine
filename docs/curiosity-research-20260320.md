# Curiosity Research Report: Skill Sovereignty & Causal Resilience
**Date**: 2026-03-20
**Status**: ANALYSIS_HARDENED

## 1. Agent Skills Spec: The Capability Contract
基於 https://agentskills.io/specification 的深度對焦，TeleNexus 的維運工具應轉向「契約化封裝」。
- **漸進式揭露 (Progressive Disclosure)**: 核心在於 `SKILL.md` 的 YAML Frontmatter。Agent 啟動時僅加載 `name` 與 `description`，大幅節省 Context。
- **目錄主權**: 每個 Skill 為一個獨立目錄，包含 `SKILL.md`、`scripts/` 與 `references/`。這理順了「工具發現」與「指令規訓」的界線。
- **對焦行動**: 應開發 `SkillGenerator` 工具，自動從 CLI 的 `--help` 輸出中生成標準化的 `SKILL.md`。

## 2. LangGraph Checkpointing: State-Machine Persistence
針對 WebDota P2P 戰局的不穩定性，LangGraph 的檢查點機制提供了「因果召回」的物理地板。
- **SQL-Based Persistence**: 利用 `SqliteSaver` 或 `PostgresSaver` 將 Graph 的每一跳狀態（Thread State）持久化。
- **戰局重連 (Match Resumption)**: 當玩家重新連線時，透過 `thread_id` 恢復最後一幀的 ECS 世界快照與決策上下文。
- **裁判代理 (Referee Agents)**: 預演使用 LangGraph 節點作為「一致性檢查點」，在 P2P 節點間進行權威共識。

## 3. C2PA & Digital Watermarking: Policy Sovereignty
對焦歐盟 8 月 AI 法案死線，實施內容憑證 (Content Credentials) 規訓。
- **Metadata Layering**: 透過 C2PA 協議將「AI 生成事實」與「主權身份」硬化至文件元數據。
- **不可篡改性**: 結合數位簽章確保智庫報告在分發過程中的因果真實性。

## 4. Rhythmic Computation: Esoteric Logic
- **I Ching & Zi Wei Algorithm**: 研究將 64 卦轉化為「狀態轉移權重」，Zi Wei 宮位轉化為「領域專注模型」。這為 AI 在極端不確定性下的「直覺式避險」提供了一套非線性的節律算法。

---
*Generated by TeleNexus Curiosity Engine.*
